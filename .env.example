# Copy this file to .env and adjust as needed for your environment.

# Base URL of the PCPS Library backend API (no trailing slash).
VITE_API_URL=

# Base URL for images/files served by the backend (cover photos, profile pictures).
# Keep the trailing slash.
VITE_RES_URL=
